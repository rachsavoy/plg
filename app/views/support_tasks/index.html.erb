<div class="tasks-contanier">
	<div class="name mb-0 pt-5">
		<h3>My Onboarding Checklist</h3>
	</div>
	<div class="checklist-task-row">
		<div class="set-up">
			<h5>SET UP ACCOUNTS</h5>
			<ul class="list-items">
				<li>Install Office 365
					<%= check_box_tag 'install_office', @support_tasks.id, @support_tasks.install_office,
						data: {
							remote: true,
							url: toggle_support_task_path(@support_tasks, params: {task: "install_office"}),
							method: "POST",
						}
				 	%> 
				</li>
				<li>RingCentral
					<%= check_box_tag 'ring_central', @support_tasks.id, @support_tasks.ring_central,
						data: {
							remote: true,
							url: toggle_support_task_path(@support_tasks, params: {task: "ring_central"}),
							method: "POST",
						}
				 	%>  
				 </li>
				<li>Adobe Pro DC
					<%= check_box_tag 'adobe', @support_tasks.id, @support_tasks.adobe,
						data: {
							remote: true,
							url: toggle_support_task_path(@support_tasks, params: {task: "adobe"}),
							method: "POST",
						}
				 	%>  
				</li>
				<li>VPN 
					<%= check_box_tag 'vpn', @support_tasks.id, @support_tasks.vpn,
						data: {
							remote: true,
							url: toggle_support_task_path(@support_tasks, params: {task: "vpn"}),
							method: "POST",
						}
				 	%>  
				</li>
				<li>Dropbox
					<%= check_box_tag 'dropbox', @support_tasks.id, @support_tasks.dropbox,
						data: {
							remote: true,
							url: toggle_support_task_path(@support_tasks, params: {task: "dropbox"}),
							method: "POST",
						}
				 	%>  
				</li>
				<li> Gusto
					<%= check_box_tag 'gusto', @support_tasks.id, @support_tasks.gusto,
						data: {
							remote: true,
							url: toggle_support_task_path(@support_tasks, params: {task: "gusto"}),
							method: "POST",
						}
				 	%>  
				</li>
			</ul>
		</div>
		<div class="set-up">
			<h5>FORMS TO FILL OUT</h5>
			<ul class="list-items">
				<li>Tax Form W-9
					<%= check_box_tag 'tax_form', @support_tasks.id, @support_tasks.tax_form,
						data: {
							remote: true,
							url: toggle_support_task_path(@support_tasks, params: {task: "tax_form"}),
							method: "POST",
						}
				 	%>  
				</li>
				<li>ACH Form
					<%= check_box_tag 'nda_form', @support_tasks.id, @support_tasks.nda_form,
						data: {
							remote: true,
							url: toggle_support_task_path(@support_tasks, params: {task: "nda_form"}),
							method: "POST",
						}
				 	%>  
				</li>
			</ul>
		</div>
		<div class="set-up">
			<h5>UPDATE/CREATE PROFILES</h5>
			<ul class="list-items">
				<li>Update all social Media 
					<%= check_box_tag 'update_social_media', @support_tasks.id, @support_tasks.update_social_media,
						data: {
							remote: true,
							url: toggle_support_task_path(@support_tasks, params: {task: "update_social_media"}),
							method: "POST",
						}
				 	%>  
				</li>
				<li>Email Signature
					<%= check_box_tag 'email_signature', @support_tasks.id, @support_tasks.email_signature,
						data: {
							remote: true,
							url: toggle_support_task_path(@support_tasks, params: {task: "email_signature"}),
							method: "POST",
						}
				 	%>  
				</li>
			</ul>
		</div>
		<div class="set-up">
			<h5>TRAINING AND ORIENTATION</h5>
			<ul class="list-items">
				<li>New Employee Training
					<%= check_box_tag 'new_employee_training', @support_tasks.id, @support_tasks.new_employee_training,
						data: {
							remote: true,
							url: toggle_support_task_path(@support_tasks, params: {task: "new_employee_training"}),
							method: "POST",
						}
				 	%>  
				</li>
			</ul>
		</div>
	</div>
</div>
